CREATE TABLE events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT NOT NULL,
    timestamp REAL NOT NULL,
    proto_data BLOB NOT NULL
);

CREATE TABLE faults (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT NOT NULL,
    timestamp REAL NOT NULL,
    severity INTEGER,
    proto_data BLOB NOT NULL
);